# 🛒 EcommerceStore Database Schema

This project defines a relational database schema for an e-commerce platform using MySQL. It includes tables for managing customers, products, categories, orders, payments, and their relationships.

---

## 📦 Features

- Customer management with contact details
- Product catalog with stock tracking and category tagging
- Order processing with itemized breakdowns
- Payment tracking with multiple methods and statuses
- Relational integrity via foreign keys and normalization

---

## 🧱 Database Setup

To create and initialize the database, run the following SQL script:

```sql
CREATE DATABASE EcommerceStore;
USE EcommerceStore;
